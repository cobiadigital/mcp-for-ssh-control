{
  "name": "mcp-server-control",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server that runs on a managed box and exposes whitelist-only operations over Streamable HTTP, reachable only through a Cloudflare Tunnel",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "smoke-test": "bash smoke-test.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  }
}
